<%= simple_form_for [@restaurant, @table, @table_order, @table_customer, @order_item] do |f| %>
  <div>
    <%= f.association :menu_item, as: :check_boxes, input_html: { multiple: true } %>
  </div>
  <div>
    <%= f.submit "Add", class: "p-2 border" %>
  </div>

<% end %>
