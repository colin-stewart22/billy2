<div class="restaurant-container">
  <div class="flex justify-between gap-8">
    <div class="w-5/12">
      <h2 class="text-2xl">Info</h2>
      <div class="h-48">
        <div class="flex mt-3">
          <h4 class="font-bold">Name</h4>
          <p class="ml-5"><%= @restaurant.name %></p>
        </div>
        <div class="flex mt-3">
          <h4 class="font-bold">Phone</h4>
          <p class="ml-5"><%= @restaurant.phone_number %></p>
        </div>
        <div class="flex mt-3">
          <h4 class="font-bold">Address</h4>
          <p class="ml-5"><%= @restaurant.address %></p>
        </div>
      </div>
    </div>
    <div class="w-48">
      <h2 class="text-2xl">Picture</h2>
      <div class="h-48 mt-3">
        <img src=<%= "https://source.unsplash.com/random/180x180/?restaurant-#{@restaurant.name}" %> class="d-block rounded-2xl" alt="restaurant image">
      </div>
    </div>
    <div class="w-48">
      <h2 class="text-2xl">Theme</h2>
      <div class="h-48 mt-3">
        <div class="flex">
          <div class="w-10 h-10 bg-blue-300 rounded-l-xl"></div>
          <div class="w-10 h-10 bg-blue-500"></div>
          <div class="w-10 h-10 bg-blue-700"></div>
          <div class="w-10 h-10 bg-blue-900 rounded-r-xl"></div>
        </div>
      </div>
    </div>
  </div>
</div>


  <h1 class="text-2xl">The name of the restaurant</h1>
  <img src=<%= "https://source.unsplash.com/random/400x380/?restaurant-#{@restaurant.name}" %> class="d-block rounded-2xl" alt="restaurant image">
  <h1><%= @restaurant.name %></h1>

  <p>This page should show details about the restaurant, including the address, phone number, all the menu-items, all the menus, the ability to add menu items, the ability to add menus, and ability to change the theme.</p>

  <div class="mt-5">
    <h1>The list of menus section</h1>
    <% @restaurant.menus.each do |item| %>
    <li> <%=link_to  item.name, restaurant_menu_path(@restaurant, item)%> </li>
    <% end %>
  </div>

  <div class="mt-5">
    <h1>The list of all menu items</h1>
    <% @restaurant.menu_items.each do |item| %>
    <%= item.name %>
    <%= item.category %>
    <%= item.price %>
    <% end %>
  </div>
